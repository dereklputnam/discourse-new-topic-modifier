rules:
  refresh: true
  default: []
  type: objects
  schema:
    name: New Topic Modification Rule
    identifier: id
    properties:
      id:
        type: string
        required: true
        validations:
          min_length: 3
      enabled:
        type: boolean
        default: true
      enabled_groups:
        type: groups
        required: true
        validations:
          min: 1
      selected_categories:
        type: categories
      include_subcategories:
        type: boolean
      button_text:
        type: string
        description: "Optional custom text for the button. Leave empty to keep default."
      icon:
        type: enum
        default: ""
        choices:
          - ""
          - "pencil"
          - "lock"
          - "ban"
          - "circle-info"
          - "envelope"
          - "flag"
          - "shield-halved"
          - "triangle-exclamation"
          - "wrench"
      tooltip_message:
        type: string
        required: true
        textarea: true
        validations:
          min_length: 5
          max_length: 500
